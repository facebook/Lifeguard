[toolchain]
channel = "nightly-2026-07-05"
# `cargo fmt` and `cargo clippy` in the cargo path of test.py need these, and
# rustup only installs them for the pinned toolchain if they are declared here.
components = ["clippy", "rustfmt"]
